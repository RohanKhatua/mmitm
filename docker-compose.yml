version: "3.8"

services:
  mmitm:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY}
      - PORT=3000
      - RUST_LOG=info
    restart: unless-stopped
